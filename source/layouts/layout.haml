!!! 5
%html
  %head
    %meta{:charset => "utf-8"}
    %meta{"http-equiv" => 'X-UA-Compatible', :content => 'IE=edge;chrome=1'}
    %title
      Blog Title #{" - #{current_article.title}" if current_article}

    = stylesheet_link_tag 'font-awesome.min'
    = stylesheet_link_tag 'bootstrap.min'
    = stylesheet_link_tag 'main'
    = javascript_include_tag 'jquery-2.1.1.min'
    = javascript_include_tag 'bootstrap.min'

    = feed_tag :atom, "#{blog.options.prefix.to_s}/feed.xml", title: "Atom Feed"

  %body
    -# HEADER
    %nav.navbar.navbar-default.navbar-fixed-top{:role => "navigation"}
      .container-fluid
        .navbar-header
          %button.navbar-toggle{:type => "button", :data => {toggle: "collapse", target: "#bs-example-navbar-collapse-1"}}
            %span.sr-only
              Toggle navigation
            %span.icon-bar
            %span.icon-bar
            %span.icon-bar
          %a.navbar-brand{:href => "/"}
            %i.fa.fa-fire.fa-lg

        .collapse.navbar-collapse#bs-example-navbar-collapse-1
          %ul.nav.navbar-nav
            -# %li
            -#   %a{:href => "/"}
            -#     About

    .main{:role => "main"}
      .row
        -# MAIN CONTENT
        .col-md-offset-1.col-md-8
          = yield
        -# Tags
        .col-md-2
          .list-group
            - blog.tags.each do |tag, articles|
              %a.list-group-item{:href => tag_path(tag), :class => ("active" if current_page.data.tag_page && tagname == tag)}
                %span.badge
                  #{articles.count}
                = "##{tag}"


      -# <ol>
      -#   <% blog.articles[0...10].each do |article| %>
      -#     <li><%= link_to article.title, article %> <span><%= article.date.strftime('%b %e') %></span></li>
      -#   <% end %>
      -# </ol>
      -#
      -# <h2>Tags</h2>
      -# <ol>
      -#   <% blog.tags.each do |tag, articles| %>
      -#     <li><%= link_to "#{tag} (#{articles.size})", tag_path(tag) %></li>
      -#   <% end %>
      -# </ol>
      -#
      -# <h2>By Year</h2>
      -# <ol>
      -#   <% blog.articles.group_by {|a| a.date.year }.each do |year, articles| %>
      -#     <li><%= link_to "#{year} (#{articles.size})", blog_year_path(year) %></li>
      -#   <% end %>
      -# </ol>
